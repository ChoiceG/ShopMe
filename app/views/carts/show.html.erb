<div class="min-h-screen bg-white py-10 px-4 sm:px-8">
    <h1 class="text-2xl font-bold mb-6">🛒 Your Shopping Cart</h1>
    <div id="errorContainer"></div>

    <div data-controller="cart" class="space-y-6 bg-gray-100 p-6 rounded shadow-md">
        <!-- Items will be dynamically inserted here by JS -->

        <div class="mt-4 text-lg font-semibold text-right" id="total"></div>

        <div class="flex justify-end space-x-4 mt-4">
            <button id="checkout-button" class="py-2 px-4 bg-gray-700 hover:bg-gray-800 rounded text-white" data-action="click->cart#checkout">
                Checkout
            </button>

            <button class="py-2 px-4 bg-red-600 hover:bg-red-700 rounded text-white" data-action="click->cart#clear">
                Clear Cart
            </button>
        </div>
    </div>
</div>